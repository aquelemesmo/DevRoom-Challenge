const chalk = require("chalk")

module.exports = () => {
    console.log(chalk.green("[SUCCESS] ") + "Bot is ready!")
    console.log(chalk.blue("[INFO] ") + "By: aquelejack")
    console.log(chalk.blue("[INFO] ") + "For: DevRoom Careers (Someone)")
}